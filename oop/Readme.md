# Rust OOP Summary (Ch. 18)

- OOP ≠ classes; Rust uses **structs + traits**.
- **Polymorphism** via *trait objects* (`dyn Trait`).
- **Encapsulation**: private fields, public methods.
- **Design patterns**: Traits replace inheritance.
