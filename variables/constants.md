#### Constants would always be annotated.

*const THREE_HOURS_IN_SECONDS: u32 = 60 * 60 * 3;*
                                |
must mention the type of const  | 

- They are only valid in the scope where they were declared.